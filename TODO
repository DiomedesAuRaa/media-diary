knowing the design like you do, what would it look like to add a List category that pulls from all the api design both books, movies, and tv? Or would it be better to have feature under each entertainment category rather than an all encompassing one